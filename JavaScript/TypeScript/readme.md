- MSの作ったJS
- tscでトランスコンパイルしてJSに
- Babelでもトランスコンパイルできるが言語ではない

# Install
```bash
$ npm -g install typescript
$ tsc -v
```

# 自動コンパイル
```bash
$ mkdir workspace
$ cd workspace
$ tsc --init
# Shitf + Ctrl + B > Watch
```
